[pytest]
testpaths = tests
norecursedirs = presto_old_code_starts
# Do not prepend the repository's parent to sys.path. With sibling editable
# checkouts such as ~/code/hitlist, prepend mode resolves the checkout root as
# an empty namespace package before the editable install's actual package.
addopts = --import-mode=importlib
